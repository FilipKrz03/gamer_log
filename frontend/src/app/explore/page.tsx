import MainContainer from "./components/MainContainer/MainContainer";
import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Explore | GamerLog",
};

export default function Explore() {
  return <MainContainer />;
}
