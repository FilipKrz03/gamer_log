"use client";
import SearchResults from "@/app/search/[searchResults]/components/SearchResults/SearchResults";

const Games = () => {
  return <SearchResults isUserData={true} />;
};

export default Games;
